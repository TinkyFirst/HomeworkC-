using System;

namespace index
{
    class Program
    {
        static void Main(string[] args)
        {
            var list = new list<>();

            var random = new Random();
            for (int i = 0; i < 10; i++)
            {
                createList.Add(random.Next(-50,50))
            }
        }
        foreach (var random in list)
        {
            Console.wrI
        }
    }
}